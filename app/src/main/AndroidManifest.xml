<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.smartsale">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuItem"></activity>
        <activity android:name=".OrderMeal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopUp"></activity>
        <activity android:name=".MainMessage" />
        <activity android:name=".Messages" />
        <activity android:name=".smartsale_dashborad" />
        <activity android:name=".ValidateCredentials" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>